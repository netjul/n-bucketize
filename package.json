{
  "name": "n-bucketize",
  "version": "1.0.3",
  "description": "This tool creates folders (buckets) per file's mime-type founded and move that file inside the bucket.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Alexis Charytonow",
  "bin": {
    "n-bucketize": "bin/n-bucketize.js"
  },
  "preferGlobal": "true",
  "license": "MIT",
  "dependencies": {
    "mime-types": "^2.1.7",
    "minimist": "^1.2.0"
  }
}
